<UserControl x:Class="Luke.Net.Features.Popup.OpenIndexView"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Popup="clr-namespace:Luke.Net.Features.Popup" mc:Ignorable="d" d:DesignWidth="394">

    <UserControl.DataContext>
        <Popup:ActiveIndexModel />
    </UserControl.DataContext>
    
	<StackPanel Margin="10" Orientation="Horizontal" VerticalAlignment="Top">
        <Label>Index Path</Label>
        <TextBox Text="{Binding Path=Path}"/>
        <RadioButton Margin="20, 0" IsChecked="{Binding Path=ReadOnly}">Readonly</RadioButton>
        <Button Content="Open Index" IsDefault="True" HorizontalAlignment="Right" Command="{Binding LoadIndexExecuted}" />
	</StackPanel>
</UserControl>
